# DP baseline (PlainConv RGB) on StackCube-v1 pd_ee_delta_pos, 990 demos.
BASE_RUN_NAME="stackcube_pos_dp_990demos_seed1"
RUN_TIMESTAMP="${RUN_TIMESTAMP:-$(date +%Y%m%d_%H%M%S)}"
RUN_NAME="${BASE_RUN_NAME}_${RUN_TIMESTAMP}"
LOG_FILE="outputs/train_logs/${RUN_NAME}.log"

TRAIN_ARGS=(
  --exp-name "${RUN_NAME}"
  --seed 1
  --env-id StackCube-v1
  --demo-path /inspire/hdd/project/robot-dna/baojiachun-CZXS25130063/zehao/4dmap/dataset/ManiSkill/StackCube-v1/motionplanning/StackCube.rgb.pd_ee_delta_pos.physx_cpu.h5
  --num-demos 990
  --control-mode pd_ee_delta_pos
  --sim-backend physx_cpu
  --obs-mode rgb
  --max-episode-steps 1000
  --total-iters 100000
  --batch-size 64
  --obs-horizon 2
  --act-horizon 8
  --pred-horizon 16
  --lr 0.0001
  --visual-encoder plain_conv
  --log-freq 100
  --eval-freq 10000
  --num-eval-episodes 100
  --num-eval-envs 10
  --save-freq 10000
  --num-dataload-workers 0
  --no-track
  --no-capture-video
)
