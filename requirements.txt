######################### cuda 12.8

# pytorch
uv pip install torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1 --index-url https://download.pytorch.org/whl/cu128
# pytorch-geometric
uv pip install torch_geometric
uv pip install pyg_lib torch_scatter torch_sparse torch_cluster torch_spline_conv -f https://data.pyg.org/whl/torch-2.7.0+cu128.html
# pytorch3d
uv pip install git+https://github.com/facebookresearch/pytorch3d.git@stable --no-build-isolation

# tensorboard
uv pip install tensorboard

# image processing
uv pip install opencv-python imageio transformations matplotlib pyrender pyOpenGL pyOpenGL_accelerate
uv pip install open3d trimesh


# clip
uv pip install ftfy regex tqdm
uv pip install git+https://github.com/openai/CLIP.git

# Grounded SAM 2
cd map4d/construction/SAM2/sam2
uv pip install -e . --no-build-isolation
cd ../../..
uv pip install transformers yapf timm supervision pycocotools # grounding dino


# foundationpose
conda install conda-forge::eigen=3.4.0 -y
uv pip install pybind11 scikit-image
python -m uv pip install --no-build-isolation "git+https://github.com/NVlabs/nvdiffrast.git"
export pybind11_DIR=/inspire/hdd/project/robot-dna/baojiachun-CZXS25130063/miniconda3/envs/4dmap/lib/python3.11/site-packages/pybind11/share/cmake/pybind11
CMAKE_PREFIX_PATH=$CONDA_PREFIX/lib/python3.10/site-packages/pybind11/share/cmake/pybind11 bash build_all_conda.sh


# diffusers
uv pip install diffusers


# lerobot
uv pip install lerobot

# others
uv pip install wandb kornia omegaconf hydra-core "zarr<3" psutil tqdm warp-lang

### benchmark
# maniskill
uv pip install mani_skill
uv pip install numpy==1.26.4


######################### cuda 11.8

# pytorch
uv pip install torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1 --index-url https://download.pytorch.org/whl/cu118
# pytorch-geometric
uv pip install torch_geometric
uv pip install pyg_lib torch_scatter torch_sparse torch_cluster torch_spline_conv -f https://data.pyg.org/whl/torch-2.7.0+cu118.html
# pytorch3d
uv pip install git+https://github.com/facebookresearch/pytorch3d.git@stable --no-build-isolation

# tensorboard
uv pip install tensorboard

# image processing
uv pip install opencv-python imageio transformations matplotlib pyrender pyOpenGL pyOpenGL_accelerate
uv pip install open3d trimesh


# clip
uv pip install ftfy regex tqdm
uv pip install git+https://github.com/openai/CLIP.git
# or using ssh
uv pip install git+ssh://org-14957082@github.com/openai/CLIP.git

# dinov3
uv pip install iopath scikit-learn-intelex submitit torchmetrics

# Grounded SAM 2
cd map4d/construction/SAM2/sam2
uv pip install -e . --no-build-isolation
cd ../../..
uv pip install transformers yapf timm supervision pycocotools # grounding dino


# foundationpose
conda install conda-forge::eigen=3.4.0 -y
uv pip install pybind11 scikit-image
python -m uv pip install --no-build-isolation "git+https://github.com/NVlabs/nvdiffrast.git"
export pybind11_DIR=$CONDA_PREFIX/lib/python3.11/site-packages/pybind11/share/cmake/pybind11
CMAKE_PREFIX_PATH=$CONDA_PREFIX/lib/python3.10/site-packages/pybind11/share/cmake/pybind11 bash build_all_conda.sh


# diffusers
uv pip install diffusers


# lerobot
uv pip install lerobot

# others
uv pip install wandb kornia omegaconf hydra-core "zarr<3" psutil tqdm warp-lang

### benchmark
# maniskill
uv pip install mani_skill
uv pip install numpy==1.26.4
